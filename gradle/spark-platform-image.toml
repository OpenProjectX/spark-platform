# Spark Platform image build configuration.
#
# Add new Spark lines, default image profiles, transitive exclusions, isolated
# variants, or capability rules here instead of changing platform-image
# build logic.

[baseImages.spark3]
repository = "ghcr.io/openprojectx/spark"
suffix = "-java17-python3-r-ubuntu"

[baseImages.spark3-scala213]
repository = "ghcr.io/openprojectx/spark"
suffix = "-java17-python3-r-ubuntu"

[baseImages.spark4]
repository = "ghcr.io/openprojectx/spark"
suffix = "-java17-python3-r-ubuntu"

[defaultVariants]
spark3 = ["iceberg", "hudi", "paimon", "openlineage"]
spark3-scala213 = ["iceberg", "openlineage"]
spark4 = ["iceberg", "hudi", "paimon", "openlineage"]

[defaultAddons]
spark3 = ["hadoopAws"]
spark3-scala213 = ["hadoopAws"]
spark4 = ["hadoopAws"]

[defaultProfiles]
spark3 = ["lakehouse"]
spark3-scala213 = ["lakehouse"]
spark4 = ["lakehouse"]

[profiles.spark3.lakehouse]
variants = ["iceberg", "hudi", "paimon", "openlineage"]
addons = ["hadoopAws", "hadoopGcs", "icebergAws"]

[profiles.spark3-scala213.lakehouse]
variants = ["iceberg", "openlineage"]
addons = ["hadoopAws", "hadoopGcs", "icebergAws"]

[profiles.spark4.lakehouse]
variants = ["iceberg", "hudi", "paimon", "openlineage"]
addons = ["hadoopAws", "hadoopGcs", "icebergAws"]

[resolution]
baseProvidedTransitiveGroups = [
    "com.google.code.findbugs",
    "org.apache.hadoop",
    "org.apache.spark",
    "org.lz4",
    "org.scala-lang",
    "org.scala-lang.modules",
    "org.slf4j",
    "org.xerial.snappy",
]

[[capabilityResolution]]
capability = "org.lz4:lz4-java"
preferredProvider = "at.yawk.lz4:lz4-java"
reason = "Paimon's Spark bundle expects at.yawk.lz4:lz4-java when both LZ4 providers are present."
